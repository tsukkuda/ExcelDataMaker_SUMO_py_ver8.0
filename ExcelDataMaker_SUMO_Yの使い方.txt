元データ
Main.py
Continuous_processing_of_csv.py
Make_Data.py
(result)

以上を同ディレクトリに設置する。"元データ"フォルダの中に交通流simから出力されるcsvファイル(実データ)を置く。連続処理可能なので、csvをたくさん置いてOK。
元データの項目として車両ID，time，position，speedは必須．
実行ファイルはMain.py．

[Main.pyについて]
9行目：R_RANGE。平均速度を計算する車群範囲の大きさの最小単位を指定。
10行目：R_total_num。指定した個数だけ、R=50[m]、100[m]、150[m]、、、と平均速度を計算して出力される。
11行目：Type_num。出力及び平均速度の計算の対象となる車両Typeを指定する。基本自動運転車両に注目するので0を指定。
12行目：Type_num。"ALL"とすることで、全ての車両Typeについてそれぞれ出力する。平均速度は車両Typeごとに計算されるが、自動運転車両以外で計算する用事がないので、"ALL"と指定することは基本無い。

[Make_Data.pyについて]
38行目：CENTER。基準点。現在位置からCENTER[m]前方の地点が範囲の中心になる。

実行時にいじる部分は基本的にない。


[2025/3/16 安田]
これは鉢嶺先輩から頂いたプログラム「エクセルデータ形成py_ver8.0」を，一部書き換えたものです．
元データを「CarSpeedPredict_SUMO_Y」で使うデータの形式に形成して出力するプsログラム．

変更点はMake_Data.pyの3~6行目参照．また，IDが文字列の場合でも動作可能になった(正直無駄な変更でした…)．
これ以外は特に変えていないので，「エクセルデータ形成py_ver8.0」を使用してもあまり問題ない．